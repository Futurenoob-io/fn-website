import { generalBackground } from './colors';

const footer = {
	background: generalBackground.footerPrimary,
	padding: '60px',
	color: 'white',
	display: 'flex',
	justifyContent: 'center'
};

const theme = {
	footer
};
export default theme;
